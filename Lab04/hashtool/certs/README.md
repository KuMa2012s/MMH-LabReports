# Certificates

Place PEM or DER certificates here for Lab 4 PKI testing.

Example:

```powershell
..\build\Release\hashtool.exe cert --in leaf.pem
..\build\Release\hashtool.exe cert --in leaf.pem --issuer issuer.pem --verify
```
